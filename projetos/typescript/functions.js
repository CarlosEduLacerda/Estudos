"use strict";
const sum = (x, y) => {
    return x + y;
};
const sub = (x, y) => {
    return x - y;
};
const value = sum(1, 2);
const log = (message) => {
    console.log(message);
    //   return 2; -> Void = Não vai retornar nada, ou seja, return vai dar erro
};
